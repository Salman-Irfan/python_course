name = "      salman     "

strip_both = name.strip()
print (strip_both)

left_strip = name.lstrip()
print (left_strip)

right_strip = name.rstrip()
print (right_strip)

# replace
replace_space = name.replace(" ", "_")
print (replace_space)